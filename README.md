Example website project for a game with an API and database stats

Steps used to build this project:
1. Set up a new Git repository for version control.
2. Since this is a "monorepo", create a folder for the website
3. In the website folder, initialize the nuxt project using `npm create nuxt@latest`
4. Nuxt configure file set to use Netlify preset for Nitro deployment
